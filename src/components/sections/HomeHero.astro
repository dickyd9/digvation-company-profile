---
import { getLocalizedPath } from '@/config/navigation';
import type { Locale } from '@/config/site';
import type { HomeCopy } from '@/content/home';

type Props = { locale: Locale; copy: HomeCopy['hero'] };
const { locale, copy } = Astro.props;
---

<section class="home-hero bg-paper-warm text-ink" data-track-section="hero">
  <div class="shell-wide home-hero__inner">
    <div class="home-hero__statement">
      <p class="home-hero__eyebrow"><span aria-hidden="true"></span>{copy.eyebrow}</p>
      <div class="home-hero__title-mask">
        <h1>{copy.title}</h1>
      </div>
    </div>

    <div class="home-hero__footer">
      <p class="home-hero__body">{copy.body}</p>
      <div class="home-hero__actions">
        <a
          class="button-primary"
          href={getLocalizedPath(locale, '/contact')}
          data-track-event="cta_click"
          data-track-label="hero-primary"
        >
          {copy.primaryCta}<span aria-hidden="true">↗</span>
        </a>
        <a class="home-hero__work" href="#featured-work">
          {copy.secondaryCta}<span aria-hidden="true">↓</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .home-hero {
    position: relative;
    min-height: calc(100svh - 4.5rem);
    overflow: clip;
  }
  .home-hero::after {
    position: absolute;
    right: 0;
    bottom: 0;
    left: 0;
    height: 1px;
    background: rgba(11, 13, 16, 0.12);
    content: '';
    transform-origin: left;
  }
  .home-hero__inner {
    display: grid;
    min-height: calc(100svh - 4.5rem);
    grid-template-rows: minmax(0, 1fr) auto;
    padding-top: clamp(4.5rem, 9vh, 7rem);
    padding-bottom: clamp(2rem, 4vh, 3.25rem);
  }
  .home-hero__statement {
    align-self: center;
  }
  .home-hero__eyebrow {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    margin: 0 0 clamp(1.6rem, 3vw, 2.6rem);
    color: rgba(11, 13, 16, 0.58);
    font-size: 0.64rem;
    font-weight: 750;
    letter-spacing: 0.11em;
    text-transform: uppercase;
  }
  .home-hero__eyebrow span {
    width: 1.8rem;
    height: 1px;
    background: var(--color-indigo-deep);
  }
  .home-hero__title-mask {
    overflow: clip;
  }
  .home-hero h1 {
    max-width: 14ch;
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(3.45rem, 5.45vw, 5.9rem);
    font-weight: 545;
    line-height: 0.95;
    letter-spacing: -0.058em;
    text-wrap: balance;
  }
  .home-hero__footer {
    display: grid;
    gap: 1.5rem 3rem;
    align-items: end;
    border-top: 1px solid rgba(11, 13, 16, 0.12);
    padding-top: 1.4rem;
  }
  .home-hero__body {
    max-width: 34rem;
    margin: 0;
    color: var(--text-muted);
    font-size: clamp(0.96rem, 1.2vw, 1.08rem);
    line-height: 1.72;
  }
  .home-hero__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.9rem 1.4rem;
  }
  .home-hero__work {
    display: inline-flex;
    min-height: 2.9rem;
    align-items: center;
    gap: 0.65rem;
    color: rgba(11, 13, 16, 0.62);
    font-size: 0.8rem;
    font-weight: 700;
    text-decoration: none;
  }
  .home-hero__work span {
    transition: transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
  }
  .home-hero__work:hover {
    color: var(--color-ink);
  }
  .home-hero__work:hover span {
    transform: translateY(0.18rem);
  }
  @media (min-width: 820px) {
    .home-hero__statement {
      display: grid;
      grid-template-columns: minmax(10rem, 0.28fr) minmax(0, 1fr);
      gap: clamp(2rem, 5vw, 5rem);
      align-items: start;
    }
    .home-hero__eyebrow {
      margin-top: 0.85rem;
      margin-bottom: 0;
    }
    .home-hero__footer {
      grid-template-columns: minmax(0, 1fr) auto;
    }
    .home-hero__actions {
      justify-self: end;
    }
  }
  @media (max-width: 640px) {
    .home-hero {
      min-height: calc(100svh - 4.25rem);
    }
    .home-hero__inner {
      min-height: calc(100svh - 4.25rem);
      padding-top: clamp(3.5rem, 10vh, 5rem);
      padding-bottom: 1.5rem;
    }
    .home-hero h1 {
      max-width: 10.5ch;
      font-size: clamp(3.15rem, 14.8vw, 4.35rem);
      letter-spacing: -0.062em;
    }
    .home-hero__body {
      max-width: 29rem;
      line-height: 1.64;
    }
    .home-hero__actions {
      align-items: flex-start;
      flex-direction: column;
    }
  }
  @media (prefers-reduced-motion: no-preference) {
    :global(html.digvation-opening) .home-hero__eyebrow {
      animation: hero-detail-in 600ms 500ms both cubic-bezier(0.22, 1, 0.36, 1);
    }
    :global(html.digvation-opening) .home-hero h1 {
      animation: hero-title-in 760ms 420ms both cubic-bezier(0.16, 1, 0.3, 1);
    }
    :global(html.digvation-opening) .home-hero__footer {
      animation: hero-footer-in 620ms 700ms both cubic-bezier(0.22, 1, 0.36, 1);
    }
    :global(html.digvation-opening) .home-hero::after {
      animation: hero-rule-in 700ms 680ms both cubic-bezier(0.22, 1, 0.36, 1);
    }
  }
  @keyframes hero-title-in {
    from {
      clip-path: inset(0 0 100% 0);
      transform: translateY(18%);
    }
    to {
      clip-path: inset(0);
      transform: translateY(0);
    }
  }
  @keyframes hero-detail-in {
    from {
      opacity: 0;
      letter-spacing: 0.16em;
    }
  }
  @keyframes hero-footer-in {
    from {
      clip-path: inset(0 100% 0 0);
    }
    to {
      clip-path: inset(0);
    }
  }
  @keyframes hero-rule-in {
    from {
      transform: scaleX(0);
    }
  }
</style>
