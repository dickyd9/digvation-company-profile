---
export const prerender = true;
import BaseLayout from '@/layouts/BaseLayout.astro';
import SolutionsIndexPage from '@/features/solutions/SolutionsIndexPage.astro';
const locale = 'en' as const;
---

<BaseLayout
  locale={locale}
  title="Solutions — Digvation"
  description="Ways to start with Digvation—from discovery and PoC to build, automation, AI/data, IoT, and care."
>
  <SolutionsIndexPage locale={locale} />
</BaseLayout>
