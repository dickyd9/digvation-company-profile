---
export const prerender = true;
import BaseLayout from '@/layouts/BaseLayout.astro';
import ProcessPage from '@/features/process/ProcessPage.astro';
const locale = 'en' as const;
---

<BaseLayout
  locale={locale}
  title="How Digvation Works | From Need to Real Use"
  description="See how Digvation understands the business need, chooses priorities with you, builds, tests, and stays involved until the solution is genuinely used."
>
  <ProcessPage locale={locale} />
</BaseLayout>
