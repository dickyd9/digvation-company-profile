---
export const prerender = true;
import BaseLayout from '@/layouts/BaseLayout.astro';
import ProcessPage from '@/features/process/ProcessPage.astro';
const locale = 'en' as const;
---

<BaseLayout
  locale={locale}
  title="How Digvation Works | From Business Context to a Working Digital Solution"
  description="See how Digvation understands business context, defines scope, builds, validates, and applies digital solutions through a clear delivery process."
>
  <ProcessPage locale={locale} />
</BaseLayout>
